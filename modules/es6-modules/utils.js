export function displayUser(user) {
  const { firstName, age, job } = user;
  console.log(`firstName = ${firstName}`);
  console.log(`age = ${age}`);
  console.log(`job = ${job}`);
}

export const age = 20;


